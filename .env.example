# Bootstrap reference only: Relay does not automatically read this file.
# Set these variables in your shell before starting the process.
# Configure models, provider keys and operational options in the web UI.
MCP_HOST=127.0.0.1
MCP_PORT=8413
FOURBIS_DB_PATH=
RELAY_OWNER_EMAIL=
CF_ACCESS_TEAM_DOMAIN=
CF_ACCESS_AUD=
LOG_LEVEL=INFO
